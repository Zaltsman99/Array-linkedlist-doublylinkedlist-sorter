The running time algorithm is O(n).
In our code, since we provide the algorithm with an already sorted array, the code only goes through the whole array one.
This means that our times complexity is equal to the size of the array. Therefore the running time is O(n).